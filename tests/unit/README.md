# Unit Tests

Fast deterministic tests for individual modules. Existing unit-style tests currently live at `tests/test_*.py`; new isolated module tests should be added here when they do not need cross-service setup.
