import Flutter

// Source-package placeholder. Release CI must regenerate this file with `flutter pub get`
// before `pod install`, `flutter build appbundle --release`, or `flutter build ipa --release`.
final class GeneratedPluginRegistrant {
  static func register(with registry: FlutterPluginRegistry) {
  }
}
