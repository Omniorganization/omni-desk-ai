# Kubernetes

Source assets:

- `deploy/kubernetes/`
- `deploy/kubernetes/helm/omnidesk/`
- `scripts/render_locked_helm_values.py`
- `scripts/check_kubernetes_contract.py`

Production values must use final OCI image digests and pass locked-values validation.
