# Connector SDK

Current source: `omnidesk_agent/channels` and `apps/shared/omni-app-api.contract.json`.

This boundary owns connector capability manifests, identity mapping, webhook verification, rate limits, idempotency, retries, and channel contract tests.
