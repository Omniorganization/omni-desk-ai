# Policy Engine

Current source: `omnidesk_agent/security`, `omnidesk_agent/privacy`, and `omnidesk_agent/validation`.

This boundary owns risk classification, approval policy, sandbox posture, signed request policy, privacy filtering, production configuration validation, and fail-closed release gates.
