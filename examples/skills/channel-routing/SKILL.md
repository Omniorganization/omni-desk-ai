# Channel Routing Skill

Use the official channel adapter when an app has configured official API credentials.
Use visible UI Bridge only for personal-account or GUI-only apps.
Never scrape cookies, bypass login, or perform hidden account automation.
Before sending a message, use the channels.send_text tool and require permission approval.
